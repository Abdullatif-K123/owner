import BranchesContent from "@/features/branches/BranchesContent";
import { pageTitle } from "@/utils/pageTitle";

const Branches = () => {
  pageTitle("الفروع");

  return <BranchesContent />;
};

export default Branches;
