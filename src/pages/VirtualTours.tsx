import VirtualToursPage from "@/features/virtualTours";
import { pageTitle } from "@/utils/pageTitle";

const VirtualTours = () => {
  pageTitle("الرحلات المجردة");
  return <VirtualToursPage />;
};

export default VirtualTours;
