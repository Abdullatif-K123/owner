import SecurityListPage from "@/features/security-list";
import { pageTitle } from "@/utils/pageTitle";

const SecurityList = () => {
  pageTitle("الأمنية");
  return <SecurityListPage />;
};

export default SecurityList;
