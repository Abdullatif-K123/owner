import AccountingDetailsPage from "@/features/accountingDetails";
import { pageTitle } from "@/utils/pageTitle";

const AccountingDetails = () => {
  pageTitle("الصناديق - تفاصيل الرحلة");
  return <AccountingDetailsPage />;
};

export default AccountingDetails;
